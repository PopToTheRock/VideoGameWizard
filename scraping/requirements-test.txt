# Lightweight dependencies for unit tests + linting, used by CI.
#
# Note: torch / chromadb / sentence-transformers are intentionally NOT here.
# server.py defers those imports to application startup (lifespan), so the
# module can be imported and tested without the heavy ML stack installed.

fastapi>=0.139.0
pydantic>=2.9
pydantic-settings>=2.4
httpx>=0.27
pytest>=8.3
ruff>=0.7
