# Full runtime dependencies for the VideoGameWizard data pipeline + RAG server.
# Python 3.11+.
#
# The server alone needs only requirements-server.txt (that is what the Docker
# image installs). This file layers the scraping pipeline on top, for a dev box
# that also (re)builds the dataset. For test/lint deps see requirements-test.txt.

-r requirements-server.txt

# --- Scraping pipeline (only needed to (re)build the dataset, not to serve) ---
requests>=2.34.2
scrapy>=2.11
beautifulsoup4>=4.15.0
